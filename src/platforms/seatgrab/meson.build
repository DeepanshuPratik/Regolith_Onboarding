# filename: src/platforms/seatgrab/meson.build
#
# Not a desktop: the mechanism three of them share. A keyboard-only seat grab
# and the synthesis dispatcher that has to release it, composed by x11, gnome
# and kde rather than reimplemented in each.
project_sources += files(
  'dispatcher.vala',
  'observer.vala'
)
