namespace regolith_onboarding {
    // This is a compile-time constant
    public const string APP_ID = "com.github.DeepanshuPratik.Regolith_Onboarding";

    // This must also be a compile-time constant, so we write it out fully.
    public const string APP_PATH = "/com/github/DeepanshuPratik/Regolith_Onboarding";
}